% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_beta_binomial}
\alias{simulate_beta_binomial}
\title{Draw a beta-binomial sample}
\usage{
simulate_beta_binomial(n, mu, sigma, n_sim = 1)
}
\arguments{
\item{n}{Positive integer scalar (or vector recycled by VGAM); number of
trials i.e. the library size.}

\item{mu}{Numeric scalar in \eqn{(0, 1)} giving the mean proportion.}

\item{sigma}{Positive numeric scalar; the dispersion parameter
(\eqn{\kappa = 1/\sigma}).}

\item{n_sim}{Positive integer; number of independent draws to produce
(default \code{1}).}
}
\value{
Integer vector of length \code{n_sim} containing simulated counts
in \eqn{\{0, 1, \dots, n\}}.
}
\description{
Draws \code{n_sim} counts from a beta-binomial distribution parameterised by
mean \code{mu} and dispersion \code{sigma}. The concentration is
\eqn{\kappa = 1/\sigma}, with shape parameters
\eqn{\alpha = \mu \kappa},
\eqn{\beta = (1-\mu) \kappa}, so higher \code{sigma} produces more
overdispersion relative to a binomial.
}
\details{
Calls \code{VGAM::rbetabinom.ab} for the actual draw.
}
\seealso{
\code{\link{simulate_compositional_bb}} which uses this routine
per (sample, taxon) cell.
}
